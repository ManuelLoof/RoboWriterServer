public static class Suggestion
{
    /// <summary>
    /// Gets the next word as suggestion.
    /// </summary>
    /// <param name="word">The previous word.</param>
    /// <returns></returns>
    public static string GetNextWord(string word)
    {
        var db = new DBProvider();
        return "";
    }
}
